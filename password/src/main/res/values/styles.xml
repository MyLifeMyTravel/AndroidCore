<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/main_color</item>
        <item name="colorPrimaryDark">@color/main_color</item>
        <item name="colorAccent">@color/main_color</item>
        <!-- 设置全局背景色 -->
        <item name="android:windowBackground">@color/main_color</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.NoActionBar.Translucent">
        <!--<item name="android:windowBackground">@android:color/transparent</item>-->
        <!--<item name="android:windowIsTranslucent">true</item>-->
    </style>

    <style name="SwitchCompatStyle" parent="AppTheme">
        <item name="colorControlActivated">#4cd964</item>
        <!-- Inactive thumb color switch关闭时的拇指按钮的颜色 -->
        <!--<item name="colorSwitchThumbNormal">@color/main_color</item>-->
        <!-- Inactive track color(30% transparency) switch关闭时的轨迹的颜色  30%这个颜色 -->
        <!--<item name="android:colorForeground">#98f3a7</item>-->
    </style>
</resources>