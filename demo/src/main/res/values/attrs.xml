<?xml version="1.0" encoding="utf-8"?>
<resources>
    <declare-styleable name="CircleProgressBar">
        <!-- 是否开启抗锯齿 -->
        <attr name="antiAlias" format="boolean" />
        <!-- 绘制内容相应的提示语 -->
        <attr name="hint" format="string|reference" />
        <attr name="hintSize" format="dimension" />
        <attr name="hintColor" format="color|reference" />
        <!-- 绘制内容的单位 -->
        <attr name="unit" format="string|reference" />
        <attr name="unitSize" format="dimension" />
        <attr name="unitColor" format="color|reference" />
        <!-- 绘制内容的数值 -->
        <attr name="maxValue" format="float" />
        <attr name="value" format="float" />
        <!-- 精度，默认为0 -->
        <attr name="precision" format="integer" />
        <attr name="valueSize" format="dimension" />
        <attr name="valueColor" format="color|reference" />
        <!-- 圆弧颜色，设置多个可实现渐变 -->
        <attr name="arcColor1" format="color|reference" />
        <attr name="arcColor2" format="color|reference" />
        <attr name="arcColor3" format="color|reference" />
        <!-- 圆弧宽度 -->
        <attr name="arcWidth" format="dimension" />
        <!-- 圆弧起始角度，3点钟方向为0，顺时针递增，小于0或大于360进行取余 -->
        <attr name="startAngle" format="float" />
        <!-- 圆弧度数 -->
        <attr name="sweepAngle" format="float" />
        <!-- 当前进度百分比 -->
        <!--<attr name="percent" format="float"/>-->
        <!-- 设置动画时间 -->
        <attr name="animTime" format="integer" />
        <!-- 背景圆弧颜色 -->
        <attr name="bgArcColor" format="color|reference" />
        <!-- 背景圆弧宽度 -->
        <attr name="bgArcWidth" format="dimension" />
    </declare-styleable>

    <declare-styleable name="CustomTextView">
        <attr name="textSize" format="dimension" />
    </declare-styleable>

    <declare-styleable name="CollapseItem">
        <!-- 默认是否展开 -->
        <attr name="collapse" format="boolean" />
        <!-- 标题 -->
        <attr name="title" format="string|reference" />
        <!-- 内容 -->
        <attr name="content" format="string|reference" />
        <!-- 提示 -->
        <attr name="tip" format="string|reference" />
        <!-- 折叠时的图标 -->
        <attr name="icon_collapse" format="reference" />
        <!-- 展开时的图标 -->
        <!--<attr name="icon_expand" format="reference" />-->
    </declare-styleable>

    <attr name="interval" format="integer" />
    <attr name="duration" format="integer" />
    <declare-styleable name="SearchImageView">
        <attr name="interval" />
        <attr name="duration" />
        <!-- 搜索效果的动画颜色 -->
        <attr name="searchColor" format="color|reference" />
    </declare-styleable>

    <declare-styleable name="WeightView">
        <!--小圆点的颜色-->
        <attr name="small_round_color" format="color|reference" />
        <!--大圆点的颜色-->
        <attr name="big_round_color" format="reference|color" />
        <!--刻度颜色-->
        <attr name="scale_color" format="reference|color" />
        <!--刻度值颜色-->
        <attr name="value_color" format="reference|color" />
    </declare-styleable>

    <declare-styleable name="SearchDeviceImageView">
        <attr name="interval" />
        <attr name="duration" />
    </declare-styleable>
</resources>